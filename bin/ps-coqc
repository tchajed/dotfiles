#!/bin/bash

# print running coqc processes and their runtime so far

SED="sed"
if command -v gsed 2>/dev/null; then
  SED="gsed"
fi

ps -ef | grep -E 'coqc .*(\w|/|-)*\.v\b' | grep -E -v '\bgrep\b' | $SED -E 's!.* ([0-9]*:[0-9.]*).* ((\w|/|-)*\.v).*!\1 \2!'
